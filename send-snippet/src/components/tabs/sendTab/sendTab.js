import React from "react";

export default function sendTab() {
  return (
    <div className="tab-content">
      <textarea></textarea>
      <button>Send</button>
      <button>Reset</button>
    </div>
  );
}
